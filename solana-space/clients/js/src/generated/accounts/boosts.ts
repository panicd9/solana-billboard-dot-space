/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const BOOSTS_DISCRIMINATOR = new Uint8Array([
  121, 188, 66, 240, 101, 221, 134, 42,
]);

export function getBoostsDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(BOOSTS_DISCRIMINATOR);
}

export type Boosts = {
  discriminator: ReadonlyUint8Array;
  /** The asset this boost record belongs to */
  asset: Address;
  /** Bump seed for this PDA */
  bump: number;
  /**
   * Unix seconds marking the start of the currently-valid Highlight window.
   * 0 = never purchased.
   * Active iff `now - highlighted_at < BOOST_DURATION_SECONDS`.
   * On re-buy while still active, this is advanced by one `BOOST_DURATION_SECONDS`
   * so the live window extends forward rather than resetting — users never lose paid time.
   */
  highlightedAt: bigint;
  /** See `highlighted_at`. */
  glowingAt: bigint;
  /** See `highlighted_at`. */
  trendingAt: bigint;
};

export type BoostsArgs = {
  /** The asset this boost record belongs to */
  asset: Address;
  /** Bump seed for this PDA */
  bump: number;
  /**
   * Unix seconds marking the start of the currently-valid Highlight window.
   * 0 = never purchased.
   * Active iff `now - highlighted_at < BOOST_DURATION_SECONDS`.
   * On re-buy while still active, this is advanced by one `BOOST_DURATION_SECONDS`
   * so the live window extends forward rather than resetting — users never lose paid time.
   */
  highlightedAt: number | bigint;
  /** See `highlighted_at`. */
  glowingAt: number | bigint;
  /** See `highlighted_at`. */
  trendingAt: number | bigint;
};

/** Gets the encoder for {@link BoostsArgs} account data. */
export function getBoostsEncoder(): FixedSizeEncoder<BoostsArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["asset", getAddressEncoder()],
      ["bump", getU8Encoder()],
      ["highlightedAt", getI64Encoder()],
      ["glowingAt", getI64Encoder()],
      ["trendingAt", getI64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: BOOSTS_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link Boosts} account data. */
export function getBoostsDecoder(): FixedSizeDecoder<Boosts> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["asset", getAddressDecoder()],
    ["bump", getU8Decoder()],
    ["highlightedAt", getI64Decoder()],
    ["glowingAt", getI64Decoder()],
    ["trendingAt", getI64Decoder()],
  ]);
}

/** Gets the codec for {@link Boosts} account data. */
export function getBoostsCodec(): FixedSizeCodec<BoostsArgs, Boosts> {
  return combineCodec(getBoostsEncoder(), getBoostsDecoder());
}

export function decodeBoosts<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<Boosts, TAddress>;
export function decodeBoosts<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<Boosts, TAddress>;
export function decodeBoosts<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<Boosts, TAddress> | MaybeAccount<Boosts, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getBoostsDecoder(),
  );
}

export async function fetchBoosts<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Boosts, TAddress>> {
  const maybeAccount = await fetchMaybeBoosts(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeBoosts<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Boosts, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeBoosts(maybeAccount);
}

export async function fetchAllBoosts(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Boosts>[]> {
  const maybeAccounts = await fetchAllMaybeBoosts(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeBoosts(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Boosts>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeBoosts(maybeAccount));
}

export function getBoostsSize(): number {
  return 65;
}
