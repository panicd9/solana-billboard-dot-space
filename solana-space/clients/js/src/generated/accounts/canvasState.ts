/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const CANVAS_STATE_DISCRIMINATOR = new Uint8Array([
  64, 117, 74, 242, 207, 52, 148, 249,
]);

export function getCanvasStateDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CANVAS_STATE_DISCRIMINATOR,
  );
}

export type CanvasState = {
  discriminator: ReadonlyUint8Array;
  /** The admin authority that initialized the canvas */
  authority: Address;
  /** Treasury wallet that receives SOL payments (mint, boost, marketplace fee) */
  treasury: Address;
  /** Metaplex Core Collection address */
  collection: Address;
  /** Total regions minted (count of NFTs) */
  totalMinted: number;
  /** Number of non-center blocks sold (drives bonding curve price) */
  curveBlocksSold: number;
  /**
   * Total blocks consumed across all minted regions (Σ width*height).
   * Drives milestone events for the giveaway. Invariant: equals popcount(bitmap).
   */
  blocksMinted: number;
  /** Bump seed for this PDA */
  bump: number;
  /**
   * Trailing padding: required to absorb implicit tail padding so the struct
   * is bytemuck::Pod-safe (no uninitialized bytes). Struct alignment = 4 (u32),
   * so the 1-byte `bump` + 2592-byte `bitmap` tail needs 3 bytes to round up.
   */
  padding: ReadonlyUint8Array;
  /**
   * Occupancy bitmap: 1 bit per block, 20736 blocks = 2592 bytes
   * bit index = y * GRID_WIDTH + x
   * 1 = occupied, 0 = free
   */
  bitmap: ReadonlyUint8Array;
};

export type CanvasStateArgs = {
  /** The admin authority that initialized the canvas */
  authority: Address;
  /** Treasury wallet that receives SOL payments (mint, boost, marketplace fee) */
  treasury: Address;
  /** Metaplex Core Collection address */
  collection: Address;
  /** Total regions minted (count of NFTs) */
  totalMinted: number;
  /** Number of non-center blocks sold (drives bonding curve price) */
  curveBlocksSold: number;
  /**
   * Total blocks consumed across all minted regions (Σ width*height).
   * Drives milestone events for the giveaway. Invariant: equals popcount(bitmap).
   */
  blocksMinted: number;
  /** Bump seed for this PDA */
  bump: number;
  /**
   * Trailing padding: required to absorb implicit tail padding so the struct
   * is bytemuck::Pod-safe (no uninitialized bytes). Struct alignment = 4 (u32),
   * so the 1-byte `bump` + 2592-byte `bitmap` tail needs 3 bytes to round up.
   */
  padding: ReadonlyUint8Array;
  /**
   * Occupancy bitmap: 1 bit per block, 20736 blocks = 2592 bytes
   * bit index = y * GRID_WIDTH + x
   * 1 = occupied, 0 = free
   */
  bitmap: ReadonlyUint8Array;
};

/** Gets the encoder for {@link CanvasStateArgs} account data. */
export function getCanvasStateEncoder(): FixedSizeEncoder<CanvasStateArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["authority", getAddressEncoder()],
      ["treasury", getAddressEncoder()],
      ["collection", getAddressEncoder()],
      ["totalMinted", getU32Encoder()],
      ["curveBlocksSold", getU32Encoder()],
      ["blocksMinted", getU32Encoder()],
      ["bump", getU8Encoder()],
      ["padding", fixEncoderSize(getBytesEncoder(), 3)],
      ["bitmap", fixEncoderSize(getBytesEncoder(), 2592)],
    ]),
    (value) => ({ ...value, discriminator: CANVAS_STATE_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link CanvasState} account data. */
export function getCanvasStateDecoder(): FixedSizeDecoder<CanvasState> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["authority", getAddressDecoder()],
    ["treasury", getAddressDecoder()],
    ["collection", getAddressDecoder()],
    ["totalMinted", getU32Decoder()],
    ["curveBlocksSold", getU32Decoder()],
    ["blocksMinted", getU32Decoder()],
    ["bump", getU8Decoder()],
    ["padding", fixDecoderSize(getBytesDecoder(), 3)],
    ["bitmap", fixDecoderSize(getBytesDecoder(), 2592)],
  ]);
}

/** Gets the codec for {@link CanvasState} account data. */
export function getCanvasStateCodec(): FixedSizeCodec<
  CanvasStateArgs,
  CanvasState
> {
  return combineCodec(getCanvasStateEncoder(), getCanvasStateDecoder());
}

export function decodeCanvasState<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<CanvasState, TAddress>;
export function decodeCanvasState<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<CanvasState, TAddress>;
export function decodeCanvasState<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<CanvasState, TAddress> | MaybeAccount<CanvasState, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getCanvasStateDecoder(),
  );
}

export async function fetchCanvasState<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<CanvasState, TAddress>> {
  const maybeAccount = await fetchMaybeCanvasState(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeCanvasState<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<CanvasState, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeCanvasState(maybeAccount);
}

export async function fetchAllCanvasState(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<CanvasState>[]> {
  const maybeAccounts = await fetchAllMaybeCanvasState(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeCanvasState(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<CanvasState>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeCanvasState(maybeAccount));
}

export function getCanvasStateSize(): number {
  return 2712;
}
